# September lesson repairs

Reviewed 2026-09-18. This records direct canonical-content corrections, not an Orchard generated-draft approval.

| Work item | Content | Correction |
| --- | --- | --- |
| 9246 | Agent architecture spectrum | Separate inner action/observation steps from bounded reflection trials; permit early termination. |
| 9247 | Agent safety and spend brakes | Four brake cases plus one recovery case; mock effects and exactly-once recovery evidence. |
| 9248 | AI compliance and receipts | Receipt issuance is distinct from deletion completion; scheduled backups keep deletion pending. |
| 9250 | DPO and model evaluation | Parseable JSON alternatives; activity compares chosen and rejected log probabilities. |
| 9251 | Function calling and tools | Actual row policy and transaction-scoped identity, runtime argument checks, downstream authorization, consistent function counts. |
| 9252 | GraphRAG | Each community report covers its community; reports collectively cover a hierarchy level. |
| 9253 | Guardrails and sandboxing | Explicit NVIDIA NeMo implementation scope within cross-provider safety instruction. |
| 9254 | Hardware and VRAM | Explicit utilization and block-size assumptions; current vLLM documentation confirms 0.92, so the old proposed default correction was rejected. |
| 9255 | LoRA and QLoRA | Scaled forward and merge equations; prerequisite modules and environment preparation. |
| 9256 | MCP | Version-specific 2026-07-28 behavior, correct primitive methods, valid JSON fragment, host-enforced isolation and subscription/state limits. The current protocol does support stateless discovery. |
| 9257 | Multi-agent orchestration | Task and role noncompliance listed separately, preserving all fourteen taxonomy modes. |
| 9258 | Open weights and quantization | Q4_K accounts for super-block fields; context includes generation; GGUF shards and multimodal components qualified. |
| 9259 | OWASP attacks | Narration, transcript, and captions aligned with plausible speech timing. |
| 9264 | Structured outputs | Strict extra-property validation; typed parse failures, nonfinal states, all-message refusal scanning, and explicit ambiguous-output rejection. |
| 9265 | Token economics | Direct Anthropic Batch citation supporting discounts and cache combination. |
| 9275 | Review agent results | Restored evidence-based review under the stable module ID and existing question IDs; preserved incident operations in a separate module and rebuilt both complete class packages. |

All fifteen standalone lessons now have matching narration/assessment captions and transcripts. Narration duration describes the recorded words, independently of classroom/activity minutes. No synthetic audio or video is claimed; the class packages remain draft text packages with their existing approval policy.

Validation: content integrity and freshness checks; twenty Node tests, including adversarial executable-example tests and caption/JSON checks; platform class-package validation for both classes. Separately executed the SQL setup with PGlite's PostgreSQL engine: two accounts with the same order number remained isolated without the application predicate, unset/expired context returned no rows, and the role could not delete. Executed the actual Zod schema against extra-key, invalid-email, and overlength payloads; all were rejected.

The SQL model assumes a trusted application session, fixed statements, and one transactional connection. It does not claim isolation from a compromised application role able to change its own account setting. Provider/live-service exercises remain learner activities; these checks did not run paid training jobs or send external messages.
